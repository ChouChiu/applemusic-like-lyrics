---
core-bundle: minor
---

feat(core): 取色新增 intent 区分强调色与主色

`createPaletteFromImage` 等新增 `intent` 选项，默认 `"accent"` 沿用 Impressionist 的取向。`"accent"` 只保留与主题色明暗一致的候选色、且亮色封面偏好更聚拢的一支，适合取强调色；`"dominant"` 不按明暗筛候选（否则 L\* 落在 (40, 60) 的中间调会被整段丢弃）、择优时一律取更分散的一支，适合铺满全屏的背景，`IsolationRenderer` 用的就是它
